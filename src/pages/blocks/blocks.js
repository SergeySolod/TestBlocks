import React, {useEffect} from 'react'
import {connect} from 'react-redux'

const Blocks = (props) => {
      return (
        <div>
            Blocks
        </div>
    )
}

export default connect()(Blocks);